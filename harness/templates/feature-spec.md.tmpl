# 機能仕様: {{FEATURE_NAME}}

> このディレクトリ（`03-features/{{FEATURE_ID}}/`）の中だけを見れば、この機能で何を作るべきかが
> わかるようにしてください。他の機能の内部実装を知る必要はありません。
> 入出力の正式な定義は `contract.yaml` です。ここはそれを人間向けに補足する文書です。

- feature_id: `{{FEATURE_ID}}`
- app_id: `{{APP_ID}}`
- 進捗状態: `status.yaml` を参照（このファイルには手書きしない）

## この機能が何をするか

(contract.yaml の description を人間向けに詳しく説明する)

## 入力・出力

正式な定義は `contract.yaml`。ここでは使用例や補足説明を書く。

## 実装方針

(必要なら。契約を満たせば実装の自由度は担当者にある)

## テスト方針

(contract.yaml の test_strategy を具体化する)

## この機能のスコープ外

(隣接機能との境界を明確にする。誤って他機能の責務を実装しないため)
